// player.cpp — the definition of print_player, declared in player.hpp.
#include "player.hpp"

#include <iostream>

void print_player(const Player& player) {
    // TODO(stage 05): print one indented line "  <name> (hp <hp>)\n" to std::cout,
    // reading player.name and player.hp from the shared struct.
    (void)player;
}
