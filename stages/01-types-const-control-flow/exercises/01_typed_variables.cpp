// Exercise 01 — Typed variables (easy)
//
// Goal: feel what "static typing" means. In C++ every variable has ONE fixed type,
// chosen when you declare it, and it keeps that type for its whole life.
//
// In Python you'd write:  level = 3        (the name 'level' could later hold a string)
// In C++ you must say:     int level = 3;   ('level' is an int forever)
//
// TODO:
//   1. Include <iostream>.
//   2. Declare these four variables (pick sensible values):
//        - an int    called  level
//        - a float   called  attack_power   (use an 'f' suffix, e.g. 12.5f)
//        - a double  called  crit_multiplier (e.g. 2.0)
//        - a bool    called  is_boss        (true or false)
//   3. Print each on its own line, like:  Level: 3
//   4. A bool prints as 1 (true) or 0 (false) by default — that's expected.
//
// Compile & run:
//   clang++ -std=c++17 -Wall -Wextra 01_typed_variables.cpp -o ex && ./ex

int main() {
    // TODO: declare the four typed variables and print them.
    return 0;
}
