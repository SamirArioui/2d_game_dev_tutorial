# Exercise 01 — your first CMakeLists (a single target)
#
# Goal: get CMake to build ONE executable named `hello` from main.cpp.
#
# Build & run (from this folder):
#   cmake -S . -B build
#   cmake --build build
#   ./build/hello
#
# TODO — write four lines:
#   1. cmake_minimum_required(VERSION 3.16)
#   2. project(ex01 LANGUAGES CXX)
#   3. set the language to C++17 (CMAKE_CXX_STANDARD 17 + CMAKE_CXX_STANDARD_REQUIRED ON)
#   4. add_executable(hello main.cpp)
#
# Compare with CMakeLists.solution.txt when you're done.

# TODO: your CMake code goes here.
