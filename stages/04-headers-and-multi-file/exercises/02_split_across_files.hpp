// SUPPORT FILE for exercise 02 — do NOT edit.
//
// This header holds only DECLARATIONS: the public face of a tiny geometry library. The
// bodies live in a .cpp file (that's the part YOU write in 02_split_across_files.cpp).
#pragma once   // include guard: paste this file's contents at most once per translation unit

// Declarations only — signatures end in a semicolon, no body.
float rect_area(float width, float height);
float rect_perimeter(float width, float height);
